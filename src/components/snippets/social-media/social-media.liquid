{%- if settings.social_enable -%}
  <div class="flex gap-x-6 {{ class }}">
    {%- if settings.social_facebook_link != blank -%}
      <a
        href="{{- settings.social_facebook_link -}}"
        class="text-accent-1"
        target="_blank"
        title="{{- 'general.social.links.facebook' | t -}}"
      >
        {%- render 'icon' with 'icon-facebook', class: 'w-6 h-6' -%}
        <span class="sr-only">{{- 'general.social.links.facebook' | t -}}</span>
      </a>
    {%- endif -%}

    {%- if settings.social_linkedin_link != blank -%}
      <a
        href="{{ settings.social_linkedin_link }}"
        class="text-accent-1"
        target="_blank"
        title="{{- 'general.social.links.linkedin' | t -}}"
      >
        {%- render 'icon' with 'icon-linkedin', class: 'w-6 h-6' -%}
        <span class="sr-only">{{- 'general.social.links.linkddin' | t -}}</span>
      </a>
    {%- endif -%}

    {%- if settings.social_instagram_link != blank -%}
      <a
        href="{{ settings.social_instagram_link }}"
        class="text-accent-1"
        target="_blank"
        title="{{- 'general.social.links.instagram' | t -}}"
      >
        {%- render 'icon' with 'icon-instagram', class: 'w-6 h-6' -%}
        <span class="sr-only">{{- 'general.social.links.instagram' | t -}}</span>
      </a>
    {%- endif -%}
  </div>
{%- endif -%}
